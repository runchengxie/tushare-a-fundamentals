# Environment variables for tushare-a-fundamentals

# TuShare token (required). Obtain from https://tushare.pro/
# The application reads this via python-dotenv or your shell environment.
TUSHARE_TOKEN=your_tushare_token_here
TUSHARE_TOKEN_2="replace-with-your-second-tushare-pro-token"

# Optional: legacy/alternative name. If set and TUSHARE_TOKEN is empty,
# the CLI will map this to TUSHARE_TOKEN automatically.
# TUSHARE_API_KEY=
